<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/integration"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                  http://www.springframework.org/schema/beans/spring-beans.xsd
                  http://www.springframework.org/schema/integration
                  http://www.springframework.org/schema/integration/spring-integration.xsd">

    <gateway id="recepcionPagosGateway"
             default-request-channel="inputPagosChannel"
             default-reply-channel="outputChannel"
             service-interface="org.fing.middleware.integration.RecepcionPagosGateway">
        <method name="procesarPagos" />
    </gateway>

    <channel id="inputPagosChannel"/>

    <splitter id="splitter"
                  ref="splitterBean"
                  method="extraerPagos"
                  input-channel="inputPagosChannel"
                  output-channel="splitterChannel"/>

    <channel id="splitterChannel"/>

    <beans:bean id="splitterBean" class="org.fing.middleware.integration.SplitterBean"/>

    <router ref="pagosRouter" method="route" input-channel="splitterChannel" />

    <beans:bean id="pagosRouter" class="org.fing.middleware.integration.PagosRouter"/>

    <aggregator input-channel="splitterChannel" method="confirmarPagos"
                    ref="recepcionPagosAggregator" output-channel="outputChannel"
                    send-partial-result-on-expiry="true">
    </aggregator>

    <beans:bean id="recepcionPagosAggregator" class="org.fing.middleware.integration.RecepcionPagosAggregator"/>

    <channel id="outputChannel"/>

    <!--chain input-channel="outputChannel">
        <header-filter header-names="content-type" />
        <service-activator expression="'from the other side'"/>
    </chain-->


    <!--aggregator input-channel="inputPagosChannel" method="confirmarPagos">
        <beans:bean class="org.fing.middleware.integration.RecepcionPagosAggregator"/>
    </aggregator-->


</beans:beans>
